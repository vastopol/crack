integers

    shift right by 1 == half
    shift left by 1  == double

strings

    rep cmps BYTE PTR ds:[esi],BYTE PTR es:[edi]
        basically a strcmp
        loops over string comparing the chars at the byte values.

